// next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['utfs.io'], // Add the utfs.io domain here for your external images
    },
};

export default nextConfig;
